services:
  vibe-carlo:
    build: .
    container_name: vibe-carlo
    restart: unless-stopped
    volumes:
      - /opt/montecarlo:/data
    networks:
      - docklinode

networks:
  docklinode:
    external: true
